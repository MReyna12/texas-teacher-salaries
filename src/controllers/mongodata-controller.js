/*const Metro = require("../../model/district-data");

const mongoData = async (req, res) => {
  //console.log(Object.keys(Metro.db.collections));
  try {
    const mongoInfo = await Metro.find();
    console.log(mongoInfo);
    res.json(mongoInfo);
  } catch (err) {
    console.error(err);
  }
};

module.exports = { mongoData };*/
