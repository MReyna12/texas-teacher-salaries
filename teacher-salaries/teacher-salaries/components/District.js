const District = (props) => {
  //console.log(props); // Brings in all the data for the metro area linked to the school district.

  return <h4>Hello there this is a district page</h4>;
};

export default District;
